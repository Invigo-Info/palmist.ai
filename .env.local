# Email Configuration for AI Palm Reader (SMTP)
# =============================================
#
# Works without any third-party API keys. For Gmail, use an App Password.

# SMTP server settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=keshav.amanile@gmail.com
SMTP_PASS=gadn bxrx finj twru

# From address shown in emails (defaults to SMTP_USER if omitted)
FROM_EMAIL="AI Palm Reader <your-email@gmail.com>"

# Admin inbox for signup notifications
ADMIN_EMAIL=keshav.amanile@gmail.com
